namespace ActivityClub.API.DTOs.Events
{
    public class UpdateEventDto : CreateEventDto
    {
        // Same fields for now (common + clean).
        // Later if needed, we can diverge (e.g., forbid changing StatusId).
    }
}
